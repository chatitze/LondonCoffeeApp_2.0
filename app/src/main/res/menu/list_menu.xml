<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:sothree="http://schemas.android.com/apk/res-auto">


    <!-- Map icon will be displayed always -->
    <!--
    <item android:id="@+id/action_map_list"
        android:icon="@drawable/ic_action_map"
        app:showAsAction="ifRoom"
        android:title="@string/show_map_menu"/>
    -->

    <item
        android:id="@+id/send_invitation"
        app:showAsAction="never"
        android:title="@string/send_invitation_menu"/>

    <item
        android:id="@+id/clear_cache"
        android:icon="@android:drawable/ic_menu_delete"
        app:showAsAction="never"
        android:title="@string/clear_cache_menu"/>


    <item
        android:id="@+id/action_toggle"
        android:orderInCategory="100"
        sothree:showAsAction="never"
        android:title="@string/action_hide"/>

    <item
        android:id="@+id/action_anchor"
        android:orderInCategory="200"
        sothree:showAsAction="never"
        android:title="@string/action_anchor_enable"/>

</menu>
